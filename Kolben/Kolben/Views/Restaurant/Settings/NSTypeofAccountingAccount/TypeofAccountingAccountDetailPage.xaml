<Page
    x:Class="Kolben.Views.Restaurant.Settings.NSTypeofAccountingAccount.TypeofAccountingAccountDetailPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Kolben.Views.Restaurant.Settings.NSTypeofAccountingAccount"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ContentControl IsEnabled="{Binding TypeofAccountingAccount.IsEditable}">
        <StackPanel Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
            <!-- Name -->
            <TextBlock Text="Nom" Margin="0 10 0 5" Style="{StaticResource BaseTextBlockStyle}" />
            <TextBox Text="{Binding TypeofAccountingAccount.Name, Mode=TwoWay}" Width="200" HorizontalAlignment="Left" />

            <!-- Prefix -->
            <TextBlock Text="Préfix de compte" Margin="0 10 0 5" Style="{StaticResource BaseTextBlockStyle}" />
            <TextBox Text="{Binding TypeofAccountingAccount.Prefix, Mode=TwoWay}" Width="200" HorizontalAlignment="Left" />
        </StackPanel>
    </ContentControl>
</Page>
